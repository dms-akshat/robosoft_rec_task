version: '3'
services:
  web:
    build: .
    ports:
      - "10000:10000"
    depends_on:
      - db
  db:
    image: postgres
    environment:
      POSTGRES_DB: analytics_db
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
